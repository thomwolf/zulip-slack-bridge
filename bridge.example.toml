database = "bridge.sqlite"
# Zulip topic for standalone messages; choose a name before first run (1–60 characters).
feed_topic = "Slack feed"  # For example: "Community lounge" or "From #general".

[slack]
team_id = "T_REPLACE_ME"
channel_id = "C_REPLACE_ME"

[zulip]
site = "https://your-organization.zulipchat.com"
channel_id = 1  # Replace with the actual numeric channel ID.
